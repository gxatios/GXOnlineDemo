//
//  PrototypeThirteenViewController.h
//  GXOnlineDemo
//
//  Created by Gx on 16/2/29.
//  Copyright © 2016年 xin gao. All rights reserved.
//
/*
 适用于对象在执行某些操作为防止意外而在执行操作前将对象状态备份的场景，有点类似于事务回滚的意思。
 下面还是先看下GOF对它的定义：
 在不破坏封装的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态。这样以后就可以就该对象恢复到原先保存的状态。
 */
#import <UIKit/UIKit.h>

@interface PrototypeThirteenViewController : UIViewController

@end
